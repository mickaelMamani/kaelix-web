import { SetPasswordForm } from "@/components/auth/set-password-form"

export const metadata = {
  title: "Définir votre mot de passe — Kaelix",
}

export default function SetPasswordPage() {
  return <SetPasswordForm />
}
